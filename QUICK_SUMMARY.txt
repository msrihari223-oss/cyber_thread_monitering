═══════════════════════════════════════════════════════════════════════════════════
                    YOUR BLOCKING SYSTEM - VISUAL SUMMARY
═══════════════════════════════════════════════════════════════════════════════════

REQUIREMENT 1: BLOCK USER WHO COMMITS BADLY ✅
═══════════════════════════════════════════════════════════════════════════════════

User Submits Toxic Content
          ↓
    POST /analyze
          ↓
   AI Analyzes Text
          ↓
   Score = 0.88 (≥ 0.85)
          ↓
   Level = "CRITICAL"
          ↓
   ████████████████████████████████████████
   ║  user.status = "blocked"  ← AUTO-BLOCKED
   ║  user.threat_score = 0.88
   ║  violation record created
   ║  email notification sent
   ████████████████████████████████████████
          ↓
   Return: "Immediate Block"
          ↓
   DATABASE UPDATED ✅


REQUIREMENT 2: BLOCKED USER CANNOT ACCESS SYSTEM ✅
═══════════════════════════════════════════════════════════════════════════════════

User Tries to Login
          ↓
    POST /login
          ↓
  Check credentials? ✅ VALID
          ↓
  ████████████████████████████████████████
  ║ if user.status == "blocked":
  ║     return HTTP 403
  ║     "Account blocked..."
  ████████████████████████████████████████
          ↓
   ❌ LOGIN DENIED
   Cannot access system
   Cannot use same credentials


REQUIREMENT 3: BLOCKED EMAIL IN ANALYTICS.HTML ✅
═══════════════════════════════════════════════════════════════════════════════════

User Opens Analytics Dashboard
          ↓
    GET /analytics (HTML Page)
          ↓
   JavaScript runs: loadAnalytics()
          ↓
    GET /admin/stats (API)
          ↓
  Database Query:
  SELECT email FROM users WHERE status="blocked"
          ↓
  Returns: ["baduser@example.com"]
          ↓
  ████████████████████████████████████████
  ║  Create blocked-item div
  ║  Display email in red box
  ║  Add to blocked-list
  ║  
  ║  Blocked Accounts:
  ║  ┌──────────────────────┐
  ║  │ baduser@example.com  │ ← SHOWN HERE
  ║  └──────────────────────┘
  ████████████████████████████████████████
          ↓
  Auto-Refresh Every 30 Seconds ✅


═══════════════════════════════════════════════════════════════════════════════════
                              CODE FLOW MAPPING
═══════════════════════════════════════════════════════════════════════════════════

FILE STRUCTURE:
┌─────────────────────────────────────────────────────────┐
│ Backend (Python FastAPI)                                │
├─────────────────────────────────────────────────────────┤
│ app/main.py                                             │
│  ├─ POST /analyze (lines 264-294)      ← BLOCKING      │
│  ├─ POST /login (lines 248-261)        ← PREVENTION    │
│  └─ GET /admin/stats (lines 297-312)   ← DATA SOURCE   │
│                                                         │
│ app/models.py                                           │
│  └─ User.status (line 16)              ← STORES STATUS │
│                                                         │
│ app/database.py                                         │
│  └─ Database connection & schema                       │
├─────────────────────────────────────────────────────────┤
│ Frontend (HTML/JavaScript)                              │
├─────────────────────────────────────────────────────────┤
│ frontend/analytics.html                                 │
│  ├─ loadAnalytics() (lines 333-373)    ← DISPLAY      │
│  └─ auto-refresh (line 377)            ← UPDATE       │
└─────────────────────────────────────────────────────────┘


REQUEST/RESPONSE FLOW:
═══════════════════════════════════════════════════════════════════════════════════

FLOW 1: SUBMITTING TOXIC CONTENT (Auto-Blocking)
┌──────────────────────────────────────────────────────────┐
│ Frontend (User submits comment in dashboard)             │
└─────────────────────────┬────────────────────────────────┘
                          │
                    POST /analyze
                    {user_id: 1,
                     comment: "toxic"}
                          │
┌─────────────────────────▼────────────────────────────────┐
│ Backend: analyze() function                              │
│ (app/main.py line 264)                                   │
├─────────────────────────────────────────────────────────┤
│ 1. Find user in database (line 269)                     │
│ 2. Call AI engine: analyze_text() (line 272)            │
│ 3. Get score & level back                              │
│ 4. Check: level == "HIGH" or "CRITICAL"? (line 274)    │
│    YES → Set user.status = "blocked" (line 276)        │
│           Send email (line 278)                         │
│           action = "Immediate Block"                    │
│ 5. Create violation record (line 283-288)              │
│ 6. Save to database (line 289)                         │
│ 7. Return response (line 292)                          │
└─────────────────────────┬────────────────────────────────┘
                          │
                    Response JSON:
                    {score: 0.88,
                     level: "CRITICAL",
                     action: "Immediate Block"}
                          │
┌─────────────────────────▼────────────────────────────────┐
│ Database: User Record Updated                            │
│ id | email | status   | threat_score                     │
│ 1  | user@ | "blocked"| 0.88  ← CHANGED                │
└──────────────────────────────────────────────────────────┘


FLOW 2: ATTEMPTING LOGIN (Prevention Check)
┌──────────────────────────────────────────────────────────┐
│ Frontend: Login page                                     │
│ User enters: email & password                            │
└─────────────────────────┬────────────────────────────────┘
                          │
                    POST /login
                    {email: "user@",
                     password: "pass"}
                          │
┌─────────────────────────▼────────────────────────────────┐
│ Backend: login() function                                │
│ (app/main.py line 248)                                   │
├─────────────────────────────────────────────────────────┤
│ 1. Find user by email (line 252-253)                    │
│ 2. Check password (line 254-255)                        │
│ 3. Check if status == "blocked" (line 256)              │
│    YES → Raise HTTP 403 error                           │
│           Message: "Account blocked..."                 │
│    NO  → Create JWT token (line 258)                    │
│           Return token                                   │
└─────────────────────────┬────────────────────────────────┘
                          │
                    Response:
                    HTTP 403 Forbidden
                    {detail: "Account blocked..."}
                          │
┌─────────────────────────▼────────────────────────────────┐
│ Frontend: Show error message                             │
│ "Account blocked due to policy violation"               │
└──────────────────────────────────────────────────────────┘


FLOW 3: CHECKING ANALYTICS (Display Blocked Emails)
┌──────────────────────────────────────────────────────────┐
│ Frontend: User visits /analytics page                    │
│ JavaScript: loadAnalytics() runs on page load            │
│ (analytics.html line 375)                                │
└─────────────────────────┬────────────────────────────────┘
                          │
                    GET /admin/stats
                          │
┌─────────────────────────▼────────────────────────────────┐
│ Backend: admin_stats() function                          │
│ (app/main.py line 297)                                   │
├─────────────────────────────────────────────────────────┤
│ 1. Count total users (line 300)                         │
│ 2. Count blocked users (line 301)                       │
│ 3. Get blocked emails list (line 304)                   │
│    blocked_emails = [user.email for user in             │
│                      db.query(User)                      │
│                      .filter(User.status=="blocked")    │
│                      .all()]                             │
│ 4. Return JSON with blocked_emails (line 305-310)       │
└─────────────────────────┬────────────────────────────────┘
                          │
                    Response JSON:
                    {total_users: 2,
                     blocked_users: 1,
                     blacklisted_users: 0,
                     blocked_emails: ["user@example.com"]}
                          │
┌─────────────────────────▼────────────────────────────────┐
│ Frontend: JavaScript processes response                  │
│ (analytics.html lines 355-362)                           │
├─────────────────────────────────────────────────────────┤
│ 1. Clear blockedList div (line 355)                     │
│ 2. If blocked_emails.length > 0:                        │
│    - For each email:                                    │
│      • Create div element                              │
│      • Set class = "blocked-item"                       │
│      • Set text = email                                │
│      • Append to blockedList                           │
│    Result:                                              │
│    <div class="blocked-item">user@example.com</div>    │
│                                                         │
│ 3. Display in page:                                     │
│    Blocked Accounts:                                    │
│    [user@example.com] ← Red bordered box                │
└─────────────────────────┬────────────────────────────────┘
                          │
                    Page Displays:
                    "Blocked Users: 1"
                    Blocked Accounts:
                    ┌────────────────────┐
                    │ user@example.com   │
                    └────────────────────┘
                          │
                    Auto-refresh set for
                    30 seconds later


═══════════════════════════════════════════════════════════════════════════════════
                            EXACT CODE LOCATIONS
═══════════════════════════════════════════════════════════════════════════════════

REQUIREMENT 1: Block User
Location: app/main.py, lines 274-280

    if level == "HIGH" or level == "CRITICAL":
        action = "Immediate Block"
        user.status = "blocked"  ← THIS LINE BLOCKS USER
        try:
            send_spam_report_email(user.email, score, level, data.comment)
        except Exception as e:
            print("Spam report email failed:", repr(e))


REQUIREMENT 2: Prevent Login
Location: app/main.py, lines 256-257

    if user.status == "blocked":  ← THIS CHECK
        raise HTTPException(status_code=403, detail="Account blocked due to policy violation")


REQUIREMENT 3: Display in Analytics
Location: app/main.py, line 304

    blocked_emails = [user.email for user in db.query(User).filter(User.status == "blocked").all()]

Location: frontend/analytics.html, lines 355-362

    data.blocked_emails.forEach(email => {
        const item = document.createElement("div");
        item.className = "blocked-item";
        item.textContent = email;  ← THIS DISPLAYS EMAIL
        blockedList.appendChild(item);
    });


═══════════════════════════════════════════════════════════════════════════════════
                              STATUS: COMPLETE ✅
═══════════════════════════════════════════════════════════════════════════════════

Your system successfully implements all 3 requirements:

✅ REQUIREMENT 1: Bad commits trigger auto-block
   Location: app/main.py:276
   Method: Sets user.status = "blocked"
   Trigger: Toxicity score ≥ 0.85

✅ REQUIREMENT 2: Blocked users cannot login
   Location: app/main.py:256
   Method: Checks user.status == "blocked" before allowing login
   Result: HTTP 403 Forbidden error

✅ REQUIREMENT 3: Blocked emails in analytics.html
   Location: app/main.py:304 + frontend/analytics.html:355-362
   Method: Fetches blocked_emails from API, displays in dashboard
   Update: Auto-refreshes every 30 seconds

🚀 PRODUCTION READY - NO CHANGES REQUIRED
